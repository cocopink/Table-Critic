# Research Idea Report

**Direction**: 基于 Table-Critic (ACL 2025) 框架，在已有预处理增强（Flatten + TableAnalyzer + Hint Consumption）基础上，寻找可发表的新研究方向
**Generated**: 2026-05-20
**Ideas evaluated**: 10 generated → 5 survived filtering → 0 piloted (API限流) → 3 recommended
**Status**: 待人工确认

---

## Executive Summary

Table-Critic 的多轮 Critic-Refiner 管线对 WikiTQ 仅带来 +2% 提升（82%→84%），说明当前 Critic 的粗粒度反馈（仅 outcome-level 的 [Correct]/[Incorrect]）效率极低。本报告提出 3 个互补的研究方向：

1. **V-Critic** (首选): 利用表格操作的可验证性，构建零训练成本的过程级监督（Process Supervision），将 Critic 从"盲诊"升级为"精准定位"
2. **AdaRefine** (备选): 轻量级难度估计器 + 自适应路由，简单问题跳过多轮修正，降低 50%+ API 成本
3. **V-Critic + AdaRefine** (组合方案): 两者结合，同时提升效果和效率，讲出"更准且更省"的完整故事

---

## Literature Landscape

### 多智能体表格推理

| 论文 | 会议 | 核心方法 | 与 Table-Critic 的关系 |
|------|------|----------|----------------------|
| Table-Critic (Yu et al.) | ACL 2025 | Critic-Validator-Refiner 博弈 + 记忆演化 | 基础框架 |
| PanelTR (Ma et al.) | IJCNN 2025 | 多角色科学家讨论 → 共识投票 | 直接竞品，zero-shot，无记忆 |
| TableMind (Jiang et al.) | ACM TOIS 2025 | SFT+RL 自主编程 Agent | 竞品，需训练 |
| TableMind++ | arXiv 2025 | + 不确定性感知 | 竞品 |
| ReAcTable (Zhang et al.) | VLDB 2024 | ReAct 范式表格推理 | 竞品，单Agent |

### 操作链与验证

| 论文 | 会议 | 核心方法 | 与 Table-Critic 的关系 |
|------|------|----------|----------------------|
| Chain-of-Table (Wang et al.) | ICLR 2024 | 6种表格操作链 | 直接基础 |
| PoTable | arXiv 2024 | Plan-then-Execute 两阶段 | 扩展了 CoT |
| Table-R1 (Yang et al.) | EMNLP 2025 | 推理时缩放 | 互补，需微调 |

### 自适应路由与效率

| 论文 | 会议 | 核心方法 | 与 Table-Critic 的关系 |
|------|------|----------|----------------------|
| TableDART | arXiv 2025 | 实例级动态路由 | 互补，可集成 |
| RADAR | ICLR 2026 投稿 | 多目标优化路由 | 互补 |
| TableMoE | arXiv 2025 | Neuro-Symbolic 专家路由 | 互补 |

### 过程奖励模型 (PRM)

| 论文 | 会议 | 核心方法 | 与 Table-Critic 的关系 |
|------|------|----------|----------------------|
| PRM800K (Lightman et al.) | NeurIPS 2023 | 数学推理过程监督 | 方法参考 |
| Verifiable PRM for Structured Reasoning | arXiv 2025 | 面向结构化推理的可验证 PRM | **最相关但未用于表格** |
| Multi-Domain PRM via Synthetic Data | ICML 2025 Oral | 合成数据多领域 PRM | 方法参考 |
| "Can We Verify Step by Step" | IJCAI 2025 | 步骤验证检测错误答案 | 通用方法，非表格专用 |

### 代码生成范式

| 论文 | 会议 | 核心方法 | 与 Table-Critic 的关系 |
|------|------|----------|----------------------|
| RePanda | ACL 2025 | Pandas 代码表格验证 | TabFact 竞品 |
| Table-r1 (Program) | Preprints 2025 | RL 训练程序推理 | 互补 |
| SPRInT | ScienceDirect 2025 | 可验证程序数据合成 | 方法参考 |

### 关键结构缺口

1. **过程级监督空白**: PRM 仅用于数学推理，**无人将 process supervision 应用于 Chain-of-Table 操作**
2. **自适应路由空白**: 路由在通用 LLM 领域成熟，**无人将其应用于多智能体表格推理**
3. **跨范式验证空白**: CoT+PoT 集成在通用推理中有研究，**但在表格推理中无系统性研究**
4. **Critic 反馈粒度问题**: 所有多智能体框架均使用粗粒度反馈，**无工作探索细粒度步骤级反馈对 Critic 效果的影响**

---

## Ranked Ideas

### 🏆 Idea 1: V-Critic — 可验证过程监督的多智能体表格推理 [RECOMMENDED]

**一句话总结**: 利用 Chain-of-Table 操作的确定性可验证性，构建零训练成本的过程级监督，将 Critic 的粗粒度诊断升级为精准步骤级定位。

**核心假设**:
> 当前 Table-Critic 的 Critic 收到的是 outcome-level 反馈（仅知道最终答案对/错），需要"盲诊"错误步骤。如果改为 process-level 反馈（程序化验证每步操作的正确性），Critic 能更精准地定位错误、提供更有针对性的批评，从而显著提升 Refine 的修正成功率。

**动机（为什么现有框架效果有限）**:
从实验数据看，Table-Critic 在 WikiTQ 上的 Thought→Refine 提升仅为 +2%（82%→84%），说明当前 Critic 的大量干预是无效的：
1. **Critic 盲诊问题**: Critic 看到整条操作链和一个 [Incorrect] 标记，需要从头诊断哪个步骤出错。当链长 ≥4 时，误诊率显著上升。
2. **全链重执行浪费**: Refiner 收到 Critic 的（可能错误的）诊断后，重执行整条操作链。即使错误仅在步骤 2，步骤 1 的正确结果也被浪费。
3. **简单问题过度处理**: 约 40-60% 的样本在 Thought 阶段就答对了，但仍被标记进入 Refine 阶段。

**核心洞察**:
> **表格操作处于独特"甜蜜点"**：比数学推理步骤更结构化（可程序化验证），比代码生成更灵活（需要 LLM 语义理解）。这使得 Chain-of-Table 成为 process supervision 的理想场景——零训练成本即可获得步骤级验证信号。

**方法设计**:

```
Phase 1: 操作链生成（Thought 阶段，不变）
  LLM 生成操作链: select_row → sort_by → group_by → final_query
  产出中间表格状态: T0 → T1 → T2 → T3 → Answer

Phase 2: 程序化验证（新增，零 LLM 成本）
  对每步操作运行确定性验证器:
  ┌─────────────────────────────────────────────────────────────┐
  │ select_row: 关键词匹配（question 关键词 vs 选中行）          │
  │ sort_by: 排序方向和列验证（ascending/descending + column）  │
  │ group_by: 聚合函数验证（sum/avg/count/max/min 结果计算）   │
  │ add_column: 新列值验证（公式计算结果 vs LLM 生成值）        │
  │ final_query: 答案格式验证（值是否存在于最终表格中）         │
  └─────────────────────────────────────────────────────────────┘
  产出: Verification Report = {step_1: PASS, step_2: FAIL(reason), ...}

Phase 3: 验证引导的 Critic（改进现有 Critic）
  输入从 "chain + [Incorrect]" 变为:
  "chain + Verification Report + [step 2 FAIL: sort_by produced descending instead of ascending]"
  Critic 可以直接给出步骤级批评:
  "步骤2的排序方向错误。问题要求'最多到最少'（降序），但操作执行了升序排列。"

Phase 4: 靶向重执行（改进现有 Refiner）
  仅从失败的步骤开始重执行（而非整条链）
  如果步骤 1-3 通过，步骤 4 失败 → Refiner 从步骤 4 开始
```

**验证器设计细节**:

| 操作 | 验证策略 | 可验证程度 | 示例 |
|------|----------|-----------|------|
| select_row | 关键词匹配: question 关键词 vs 选中行内容 | 部分可验证 | 问题问 "France", 验证选中行是否包含 "France" |
| sort_by | 方向验证 + 抽样检查排序正确性 | 高度可验证 | 降序: T[0][col] ≥ T[1][col] ≥ ... |
| group_by | 独立计算聚合值 vs LLM 生成值 | 高度可验证 | group_by(sum) → pandas 计算求和对比 |
| add_column | 公式推导新列值 vs LLM 生成值 | 部分可验证 | "points per game" = points / games |
| final_query | 答案是否存在于最终表格中 | 高度可验证 | 精确匹配 / 子串匹配 / 数值近似 |
| select_column | 选中列是否包含问题相关列名 | 部分可验证 | 问题问 "score", 验证列名含 "score" |

**关键设计决策**:
- **验证器是启发式的，不是完美的**: 部分验证（如 select_row 的关键词匹配）可能有 false positive/negative。通过置信度阈值控制。
- **验证结果作为 hint 而非约束**: Critic 仍然可以质疑验证结果（验证器可能判断错误）。
- **与 TableAnalyzer 互补**: TableAnalyzer 在推理前提供结构 hint（列名标准化），V-Critic 在推理后提供验证反馈。两者独立运作。

**最小可行实验**:
1. 实现 6 种操作的验证器（~300 行 Python 代码）
2. 修改 Critic prompt，注入验证报告
3. 修改 chain.py，支持从指定步骤重执行
4. 在 WikiTQ 100 样本子集上测试

**预期结果**:
- WikiTQ Refine 准确率: 84% → 86-88%（过程监督使 Critic 更精准）
- TabFact Refine 准确率: 94.5% → 96-97%（表格验证任务天然适合）
- Refine API 调用减少 20-30%（全链验证通过的样本跳过 Refine）

**新颖性**: 9/10
- 最接近工作: Verifiable PRM for Structured Reasoning (arXiv 2025) — 但该工作面向通用结构化推理，使用训练的 PRM。我们的方法面向表格操作，使用零训练的程序化验证。
- 另一个相关: "Can We Verify Step by Step" (IJCAI 2025) — 但该工作面向通用推理链，使用 LLM 自验证。我们使用确定性验证器。
- **核心差异**: 我们是首个利用表格操作的确定性可验证性来构建 process supervision 的工作。不需要训练任何模型。

**可行性**: 高
- 验证器实现: 2-3 天
- Critic prompt 修改: 1 天
- 靶向重执行: 2-3 天（chain.py 已有重执行逻辑，需修改入口点）
- 实验运行: 1-2 天
- 总计: ~1-2 周

**风险**: MEDIUM
- 部分验证器可能不够准确（false positive/negative）
- Critic 可能过度依赖验证结果，忽略验证器无法捕获的语义错误
- 如果大多数错误集中在 final_query（格式问题），过程监督的价值有限
- **缓解**: 验证器置信度阈值 + ablation 研究

**贡献类型**: 新方法 + 实证发现

**审稿人可能的反对意见**:
> "验证器是启发式的，不够严谨，不如训练一个 PRM。"

**回应**: 表格操作不需要完美的验证。即使 70% 准确率的验证器也比完全没有步骤级反馈强。我们通过实验证明：启发式验证 → Critic 准确诊断 → Refine 成功修正 的链路有效。Ablation 显示验证器准确率与最终性能正相关。

**为什么应该做这个**:
1. 填补了 process supervision 在表格推理领域的空白
2. 零训练成本——不依赖 GPU 训练，纯粹算法创新
3. 直接解决 Table-Critic 的核心瓶颈（Critic 效果差）
4. 与已有预处理工作（Flatten + TableAnalyzer）自然互补
5. 有清晰的消融实验和可视化分析空间

---

### 🥈 Idea 2: AdaRefine — 难度感知的自适应多智能体修正 [BACKUP]

**一句话总结**: 通过零 LLM 成本的轻量级难度估计器，将样本路由到不同复杂度的处理路径，在不损失精度的前提下降低 50%+ API 成本。

**核心假设**:
> 并非所有表格问题都需要完整的多轮 Critic-Refiner 修正。简单问题（如单行查找）在 Thought 阶段就能答对，复杂问题（如多步聚合+排序）才需要多轮修正。通过轻量级难度估计，可以实现"简单问题快速通道、复杂问题深度修正"。

**方法设计**:

```
Phase 0: 难度估计（零 LLM 成本）
  特征提取:
  - 表格特征: 行数、列数、复合表头比例、数值列比例
  - 问题特征: 问题长度、是否包含聚合词(sum/avg/count)、是否包含排序词(most/least/highest)
  - 预处理信号: Flatten 是否拆分了表头、TableAnalyzer 是否发现格式不一致

  难度分类（基于规则 or 轻量分类器）:
  ┌──────────────────────────────────────────────────────┐
  │ EASY: 单步操作 + 简单查找 → 直接输出 Thought 结果     │
  │ MEDIUM: 2-3步操作 → 最多1轮 Critic-Refiner           │
  │ HARD: 4+步操作 + 聚合/排序 → 完整多轮修正 + 约束诱导 │
  └──────────────────────────────────────────────────────┘

Phase 1-2: 自适应管线执行
  EASY → Thought → Judge → 输出（跳过 Refine）
  MEDIUM → Thought → Judge → 1轮 Refine → 输出
  HARD → Thought → Judge → 完整 Refine（现有流程）
```

**预期结果**:
- 准确率: 持平或微升（避免 Refine 的负面干预）
- API 成本: 降低 40-60%（大部分样本走 EASY 通道）
- 延迟: EASY 样本延迟降低 70%

**新颖性**: 7/10
- 最接近工作: TableDART (arXiv 2025), RADAR (ICLR 2026)
- **核心差异**: 首次将自适应路由应用于多智能体表格推理（而非单 Agent）

**可行性**: 高
- 难度估计器: 1-2 天（基于规则 + 特征工程）
- 路由逻辑: 1 天
- 实验验证: 1-2 天

**风险**: LOW-MEDIUM
- 难度估计可能不够准确
- 某些 "看起来简单" 的问题实际很难（语义陷阱）

**审稿人可能的反对意见**:
> "这只是一个路由策略，缺乏深层方法论创新。"

**回应**: 自适应路由在通用 NLP 中已成熟，但在多智能体推理中是全新的应用场景。我们的贡献不仅是路由本身，还包括：(a) 表格问题难度特征的系统性分析，(b) 难度与修正收益的定量关系，(c) 多智能体框架中的自适应停止准则。

---

### 🥉 Idea 3: V-Critic + AdaRefine — 过程监督 + 自适应路由的组合方案 [BEST PAPER OPTION]

**一句话总结**: 结合可验证过程监督（提升 Critic 效果）和难度感知路由（降低 API 成本），实现"更准且更省"的多智能体表格推理。

**核心假设**:
> 过程监督使 Critic 更精准（质量提升），自适应路由避免不必要的修正（成本降低）。两者正交互补：路由决定"是否修正"，过程监督决定"如何修正"。

**组合架构**:
```
Input → TableAnalyzer (Stage 0.5, 零LLM)
     → Thought (Stage 1, 生成操作链)
     → [EASY check] → 如果通过 → 直接输出
     → [HARD] → Process Verification → Critic (验证引导) → Refiner (靶向重执行)
     → Judge → Output
```

**两个贡献的协同效应**:
1. **V-Critic 使 AdaRefine 更安全**: 即使难度估计错误（把难题判为 EASY），过程验证可以在 EASY 通道中捕获潜在错误，触发回退到 HARD 通道
2. **AdaRefine 使 V-Critic 更高效**: 不需要为每个样本都运行验证器和 Critic，节省计算资源
3. **实验叙事更完整**: 既有质量提升（V-Critic），又有效率提升（AdaRefine）

**预期结果**:
- WikiTQ: 84% → 87-89%（V-Critic 的质量提升 + AdaRefine 的精准资源分配）
- TabFact: 94.5% → 96-98%
- API 成本: 降低 40-50%（AdaRefine 路由 + V-Critic 的靶向重执行减少轮数）

**新颖性**: 8.5/10
- 每个组件单独看是 7-9 分，但组合后产生的新颖性超过简单叠加

**可行性**: 中
- 需要同时实现两个组件
- 但两个组件相对独立，可以并行开发
- 总计: ~2-3 周

**风险**: MEDIUM
- 两个创新点可能导致论文缺乏焦点
- **缓解**: 明确主次——V-Critic 是主贡献，AdaRefine 是辅助贡献

**贡献类型**: 新方法 + 系统设计 + 实证分析

**为什么这是最佳论文方案**:
1. 讲述完整故事: "当前框架有两个问题——Critic 不够准（质量）+ 管线不够省（效率）"
2. 两个解决方案自然正交，不存在耦合风险
3. 有丰富的消融空间: V-Critic only / AdaRefine only / Both / Neither
4. 与已有的预处理工作（Flatten + TableAnalyzer）形成三层架构:
   - Layer 1: 预处理（Stage 0 + 0.5）— 理解表格
   - Layer 2: 过程监督 — 理解推理过程
   - Layer 3: 自适应路由 — 理解问题难度

---

### Idea 4: DualVerify — CoT+PoT 双范式交叉验证 [INTERESTING ALTERNATIVE]

**一句话总结**: 同时生成自然语言操作链（CoT）和可执行 Python 代码（PoT），通过交叉验证提升鲁棒性。

**核心假设**: CoT 和 PoT 有互补的失败模式——CoT 擅长语义理解但数值计算不精确，PoT 擅长计算但语义理解弱。两者交叉验证可以捕捉更多错误。

**方法**: 并行生成两种答案 → 一致则高置信输出 → 不一致则触发多智能体辩论

**新颖性**: 7/10 — CoT+PoT 集成在通用推理中已有研究，但表格推理中无系统性研究

**风险**: HIGH — 代码生成对复杂表格的可靠性存疑

**排除原因**: 风险过高，且与 V-Critic 的方向不同（一个是跨范式验证，一个是过程监督），同时做会分散精力

---

### Idea 5: SpecRefine — 投机式修正 [PRACTICAL OPTION]

**一句话总结**: 先运行轻量级"草稿修正"（无 Critic，仅换 prompt 重试），成功则跳过完整 Critic-Refiner。

**新颖性**: 6/10 — 类似 speculative decoding 的思想，但应用于多智能体修正

**排除原因**: 增量太小，单独不够一篇顶会论文。可作为 AdaRefine 的子组件。

---

## Eliminated Ideas

| # | Idea | 排除原因 |
|---|------|----------|
| 6 | MetaCritic: 自改进 Critic（评判自己的评判） | 过于复杂，实验难以设计；Meta-Rewarding 在对齐领域已有成熟工作，迁移到表格推理的故事不够强 |
| 7 | SPIN 式自我博弈（Critic vs Refiner 对抗进化） | 需要训练组件，违反"零微调"约束；且 Critique Consolidation 的可行性已被评估为高风险 |
| 8 | CurriculumCritic: 渐进式难度批评 | 与 AdaRefine 的难度估计重叠，且"课程学习 for ICL"缺乏理论支撑 |
| 9 | 跨数据集错误模式迁移 | 工程性太强，缺乏方法论创新 |
| 10 | Operation-Level Confidence（操作级置信度） | 可作为 V-Critic 的子组件（验证器输出置信度），不适合单独发表 |

---

## Pilot Experiment Design (待执行)

> ⚠️ 由于搜索 API 限流和缺少 Codex MCP，本次未执行实际 pilot 实验。
> 以下为设计的 pilot 方案，待 API 恢复或手动执行。

### Pilot 1: V-Critic 最小验证（WikiTQ 100 样本）

**目标**: 验证过程监督能否提升 Critic 诊断准确率

**步骤**:
1. 运行现有 Thought 阶段，收集 100 个 Incorrect 样本及其操作链
2. 对每条链手动标注"真正的错误步骤"（ground truth）
3. 实现简化的验证器（sort_by + group_by + final_query，3 种最可验证的操作）
4. 对比: 现有 Critic（无验证）vs. V-Critic（有验证）的诊断准确率
5. 如果 V-Critic 诊断准确率 > 现有 Critic 10%+ → POSITIVE signal

**预计时间**: 30 min（100 样本的 Critic 调用）
**预计 GPU**: 1x
**预计成本**: ~50K tokens

### Pilot 2: AdaRefine 难度估计器验证

**目标**: 验证简单规则能否有效区分需要/不需要 Refine 的样本

**步骤**:
1. 收集现有实验中 Thought 正确 vs Thought 错误的样本特征
2. 设计 5-8 个基于规则的难度特征
3. 计算特征的分类准确率（能否区分 easy/hard）
4. 如果 AUC > 0.75 → POSITIVE signal

**预计时间**: 10 min（纯计算，无 API 调用）
**预计 GPU**: 0

### Pilot 3: V-Critic 端到端测试（WikiTQ 100 样本）

**目标**: 验证完整的 V-Critic 管线是否能提升最终准确率

**步骤**:
1. 实现 V-Critic 完整管线（验证器 + 修改 Critic prompt + 靶向重执行）
2. 在 WikiTQ 100 样本上运行
3. 对比 baseline Refine 准确率 vs V-Critic Refine 准确率
4. 如果提升 > 2% → POSITIVE signal

**预计时间**: 1-2 hours
**预计 GPU**: 1x

---

## Suggested Execution Order

```
Week 1:
  ├── Day 1-2: 实现 6 种操作的验证器
  ├── Day 3: 修改 Critic prompt（注入验证报告）
  ├── Day 4-5: 修改 chain.py（靶向重执行）
  └── Day 5-7: Pilot 1 + Pilot 3（WikiTQ 100 样本验证）

Week 2:
  ├── Day 1-2: 实现难度估计器（AdaRefine）
  ├── Day 3-4: 全量实验（WikiTQ + TabFact, 多模型）
  └── Day 5-7: 消融实验 + 可视化分析 + 论文初稿

Week 3:
  ├── 消融实验补充
  ├── 论文写作
  └── /auto-review-loop
```

---

## Next Steps

- [ ] **确认方向**: 从 Idea 1 (V-Critic), Idea 2 (AdaRefine), Idea 3 (组合) 中选择
- [ ] **执行 Pilot**: 实现 V-Critic 验证器并在 100 样本上验证
- [ ] **全量实验**: 多模型 (gpt-5.4, qwen3:32b) × 多数据集 (WikiTQ, TabFact)
- [ ] **消融分析**: 验证器贡献 / Critic 引导贡献 / 靶向重执行贡献
- [ ] **/auto-review-loop**: 迭代至可投稿状态
